---
title: "PHP 7.4 после 7.3: типизированные свойства, arrow functions, FFI и миграция | DevSense"
description: "Гайд по переходу на PHP 7.4: typed properties, arrow functions, вариантность, ??=, spread в массивах, WeakReference, __serialize/__unserialize, OPcache preload, FFI и обратная несовместимость из практики."
faq:
  - question: "Что такое типизированные свойства в PHP 7.4?"
    answer: "В PHP 7.4 разрешено объявлять типы для свойств классов. Попытка прочитать типизированное свойство до того, как ему присвоено значение, вызовет фатальную ошибку (TypeError)."
  - question: "Как работают стрелочные функции (arrow functions) в PHP 7.4?"
    answer: "Стрелочные функции используют ключевое слово 'fn' и автоматически захватывают переменные из родительской области видимости по значению. Они ограничены одним выражением, которое неявно возвращается."
  - question: "Что такое OPcache preloading (предзагрузка)?"
    answer: "Предзагрузка позволяет загружать PHP-файлы в память при запуске сервера, делая их доступными для всех запросов без накладных расходов на компиляцию, но требуя перезапуска PHP-FPM для обновления кода."
  - question: "Что за новый механизм кастомной сериализации появился в PHP 7.4?"
    answer: "Введены магические методы '__serialize' и '__unserialize', которые заменяют устаревший интерфейс 'Serializable' и обеспечивают более надежное управление сериализацией объектов."
---

# PHP 7.4: Главные нововведения

Представьте, что вы обновили версию PHP, и ваш продакшн упал просто потому, что свойство класса не было инициализировано. PHP 7.4 приносит долгожданную строгую типизацию свойств классов, но с этой силой приходит и жесткое требование: любое типизированное свойство должно быть инициализировано до первого чтения. Эта версия служит главным мостом к современному PHP 8, внедряя типизированные свойства и стрелочные функции, которые требуют дисциплины от разработчика.

## Содержание (Оглавление)
* [Типизированные свойства](#typed-properties)
* [Arrow functions](#arrow-functions)
* [Ковариантность и контравариантность](#variance)
* [Оператор присваивания с null coalescing (`??=`)](#null-coalesce-assign)
* [Оператор spread в массивах](#array-spread)
* [Разделитель в числовых литералах](#numeric-separator)
* [Слабые ссылки (WeakReference)](#weak-references)
* [Исключения из `__toString()`](#tostring-exceptions)
* [Сериализация: `__serialize` / `__unserialize`](#custom-serialize)
* [Расширения и стандартная библиотека](#extensions-stdlib)
* [Практические рецепты](#practical-recipes)
* [Частые ошибки](#common-mistakes)
* [Ограничения](#limitations)
* [Обратно несовместимые изменения (миграционные заметки)](#backward-incompatible)
* [Deprecated (исправить заранее)](#deprecations)
* [Прочие изменения и эксплуатация (сборка, INI, perf)](#other-changes)
* [Интерактивный квиз](#interactive-quiz)
* [Итог](#closing-thoughts)

---

<a id="typed-properties"></a>
## Типизированные свойства

У свойств класса можно объявлять типы. **Неинициализированное** typed property **нельзя читать** до присваивания — будет выброшена ошибка **`Typed property ... must not be accessed before initialization`**.

```php
// src/User.php
final class User
{
    public int $id;
    public string $name;
    public ?string $nickname = null;
}
```

Из практики:

* Если «нет значения» допустимо — используйте **`?Type`** или значение по умолчанию.
* Тип **`callable`** для свойств **запрещён** (контекстно-зависимое поведение).
* Объектные typed properties имеют ограничения на инициализацию на уровне объявления — см. мануал.

> [!NOTE]
> А вы знали? В PHP 7.4 типизированные свойства реализованы на уровне движка с защитой «ленивой инициализации». Свойство находится в состоянии «uninitialized» (неинициализировано), что отличается от `null`. Проверка `isset($user->id)` вернет `false`, если свойство не было задано, даже если тип не допускает `null`.

<a id="arrow-functions"></a>
## Arrow functions

Arrow functions (`fn`) захватывают переменные из окружения **по значению** и возвращают одно выражение — удобно для коротких колбэков.

```php
// src/MathHelper.php
$factor = 10;
$nums = array_map(fn($n) => $n * $factor, [1, 2, 3]);
```

**`fn` — зарезервированное слово**: нельзя назвать функцию или класс `fn` (имена методов/констант допускаются).

> [!NOTE]
> А вы знали? В отличие от стрелочных функций в JavaScript, в PHP переменные захватываются исключительно по значению. Изменение переменной внутри стрелочной функции не повлияет на её значение во внешней области видимости.

<a id="variance"></a>
## Ковариантность и контравариантность

Типы возвращаемых значений могут **сужаться** (ковариантность), типы параметров — **расширяться** (контравариантность) при наследовании, с ограничениями. Полная вариантность лучше работает при **autoload**; в одном файле допустимы только **нециклические** ссылки на классы.

```php
// src/Repository.php
interface Repository {
    public function get(int $id): object;
}

class UserRepository implements Repository {
    public function get(int $id): User { // Ковариантный тип возврата
        return new User();
    }
}
```

<a id="null-coalesce-assign"></a>
## Оператор присваивания с null coalescing (`??=`)

Присваивание выполняется, если слева **не задано** или **`null`**:

```php
// src/Config.php
$config['debug'] ??= false;
```

<a id="array-spread"></a>
## Оператор spread в массивах

Распаковка итерируемых значений внутри литерала массива:

```php
// src/ArrayMerge.php
$base = [1, 2];
$all = [0, ...$base, 3]; // [0, 1, 2, 3]
```

Хорошо сочетается с `array_merge(...$arrays)`: **`array_merge()`** без аргументов теперь возвращает **`[]`**.

<a id="numeric-separator"></a>
## Разделитель в числовых литералах

Подчёркивания улучшают читаемость и **не меняют** значение:

```php
// src/Constants.php
$million = 1_000_000;
$hex = 0xFF_FF_FF;
```

<a id="weak-references"></a>
## Слабые ссылки (WeakReference)

`WeakReference` держит ссылку, которая **не удерживает** объект от сборки мусора — полезно для кэшей и графов без утечек.

```php
// src/Cache.php
$obj = new stdClass();
$weakRef = WeakReference::create($obj);
```

<a id="tostring-exceptions"></a>
## Исключения из `__toString()`

Бросать исключения из `__toString()` разрешено (раньше — fatal). Часть прежних recoverable fatals стала **`Error`** — проверьте пути приведения к строке.

```php
// src/Stringy.php
class Stringy {
    public function __toString(): string {
        throw new Exception("Failed to cast to string");
    }
}
```

<a id="custom-serialize"></a>
## Сериализация: `__serialize` / `__unserialize`

Новая пара методов заменяет разрозненные приёмы и со временем вытесняет `Serializable`:

```php
// src/Point.php
final class Point
{
    private int $x;
    private int $y;

    public function __serialize(): array
    {
        return ['x' => $this->x, 'y' => $this->y];
    }

    public function __unserialize(array $data): void
    {
        $this->x = $data['x'];
        $this->y = $data['y'];
    }
}
```

Типы SPL вроде `ArrayObject` могут писать **новые** сериализованные представления, читаемые на 7.4+, но не на более старых PHP.

<a id="extensions-stdlib"></a>
## Расширения и стандартная библиотека

* **FFI**: вызов нативных C-библиотек из PHP (мощно; нужны модель угроз и ограничения доступа).
* **OPcache preloading**: загрузка набора файлов при старте — `opcache.preload`, часто `opcache.preload_user`.
* **`mb_str_split()`**: как `str_split()`, но по **кодпоинтам**.
* **`proc_open()`**: команда **массивом** (без shell), дескрипторы `redirect` / `null`.
* **`strip_tags()`**: разрешённые теги **массивом** имён.
* **PDO**: логин/пароль в DSN для большего числа драйверов; **`??`** в SQL экранирует литеральный `?` (например, JSON-операторы в PostgreSQL).
* **PCRE**: у `preg_replace_callback*` появился аргумент `flags` (`PREG_OFFSET_CAPTURE`, `PREG_UNMATCHED_AS_NULL`).
* **Password**: Argon2 через **sodium**, если сборка без libargon.

<a id="practical-recipes"></a>
## Практические рецепты

### Безопасные дефолты с `??=`

```php
// src/recipes.php
$options['timeout'] ??= 30;
```

### Arrow function в сортировке

```php
// src/recipes.php
usort($rows, fn($a, $b) => $a['score'] <=> $b['score']);
```

### Spread-merge динамических списков

```php
// src/recipes.php
$merged = array_merge(...$chunks);
```

<a id="common-mistakes"></a>
## Частые ошибки

Ниже приведены типичные ошибки разработчиков при переходе на PHP 7.4:

### 1. Чтение неинициализированного свойства
```php
// src/BadPropertyAccess.php
// Плохо: обращение к типизированному свойству до его инициализации
class BadUser {
    public int $id;
}
$user = new BadUser();
echo $user->id; // Fatal Error: Typed property BadUser::$id must not be accessed before initialization
```
```php
// src/GoodPropertyAccess.php
// Хорошо: укажите значение по умолчанию или инициализируйте в конструкторе
class GoodUser {
    public int $id = 0;
}
```

### 2. Попытка изменить внешние переменные в стрелочных функциях
```php
// src/BadArrowScope.php
// Плохо: ожидание, что внешняя переменная изменится по ссылке
$count = 0;
$increment = fn() => $count++;
$increment();
echo $count; // Вывод: 0 (Внешняя переменная не изменилась!)
```
```php
// src/GoodArrowScope.php
// Хорошо: используйте традиционную анонимную функцию с `use (&$count)`
$count = 0;
$increment = function() use (&$count) {
    $count++;
};
$increment();
echo $count; // Вывод: 1
```

<a id="limitations"></a>
## Ограничения

* **Стрелочные функции**: могут содержать только одно выражение. Вы не можете использовать многострочную логику или явный оператор `return`.
* **OPcache Preloading**: предзагруженные файлы кэшируются намертво. Любые изменения в коде требуют полной перезагрузки процесса PHP-FPM или веб-сервера.
* **FFI**: даёт максимальную производительность, но отключает защитные слои PHP. Ошибки в коде могут приводить к Segmentation Fault (падению процесса PHP).

<a id="backward-incompatible"></a>
## Обратно несовместимые изменения (миграционные заметки)

### Ядро

* **Доступ как к массиву у не-массивов**: для `null`, `bool`, `int`, `float`, `resource` вида `$x['k']` — **notice**.
* **`get_declared_classes()`**: не возвращает **анонимные** классы, пока они не инстанциированы.
* **`fn`**: зарезервировано для имён функций/классов.
* **`<?php` в конце файла** без перевода строки: теперь это **открывающий тег** (раньше поведение зависело от `short_open_tag`).
* **Stream wrappers**: при `include`/`require` по stream может вызываться `stream_set_option(STREAM_OPTION_READ_BUFFER)` — реализуйте метод или верните `false`, чтобы не ловить warning.
* **Сериализация**: формат **`o`** удалён (актуально для искусственных строк).
* **Константы паролей**: `PASSWORD_*` — **строки** (`'2y'`, `'argon2id'`, …), не целые — код сравнения с `1`/`2`/`3` ломается.
* **`htmlentities()`**: **notice**, если кодировка сводится к поведению `htmlspecialchars`.
* **`fread`/`fwrite`**: при ошибке **`false`** (не `''`/`0`); возможен **notice**.
* **BCMath**: предупреждение при «кривых» числовых строках (как раньше, интерпретируется как ноль).
* **CURL**: сериализация **`CURLFile`** бросает раньше; нестандартный аргумент `curl_version()` — предупреждение/игнор.
* **Date**: дамп `DateTime*` не оставляет «лишних» свойств; сравнение **`DateInterval`** предупреждает и даёт **`false`**.
* **Intl**: дефолт для `idn_to_ascii` / `idn_to_utf8` — **`INTL_IDNA_VARIANT_UTS46`**.
* **MySQLi**: убран embedded server; свойство `$mysqli->stat` — используйте **`mysqli::stat()`**.
* **OpenSSL**: `openssl_random_pseudo_bytes` бросает как **`random_bytes`** при ошибках; `$crypto_strong` гарантированно `true`, если исключения не было.
* **PCRE**: с **`PREG_UNMATCHED_AS_NULL`** хвостовые несовпавшие группы — **`null`** (стабильный размер `$matches`).
* **PDO**: сериализация **`PDO`/`PDOStatement`** — **`Exception`** (не `PDOException`).
* **Reflection**: сериализация объектов reflection **запрещена** явно; изменились **числовые значения** некоторых констант модификаторов.
* **SPL / `ArrayObject`**: изменилось поведение **`get_object_vars`** без `STD_PROP_LIST`; `SplPriorityQueue::setExtractFlags(0)` бросает сразу.
* **Tokenizer**: неожиданные байты — **`T_BAD_CHARACTER`**.
* **Cookies (с 7.4.11)**: имена cookie **не** URL-декодируются.

<a id="deprecations"></a>
## Deprecated (исправить заранее)

Главное:

* **Вложенные тернарные операторы** без явных скобок (кроме однозначной формы с вложением в средний операнд).
* **Фигурные скобки** для смещения `$str{0}` → **`[]`**.
* **`(real)`** / **`is_real()`** → **`(float)`** / **`is_float()`**.
* **`array_key_exists()` для объектов** → **`isset()`** / **`property_exists()`**.
* **`implode($parts, $glue)`** → **`implode($glue, $parts)`**.
* **`ReflectionType::__toString()`** и **`Reflection*::export()`** → API вроде **`ReflectionNamedType::getName()`** и строковое приведение reflection-объектов.
* **`allow_url_include`**, **`FILTER_SANITIZE_MAGIC_QUOTES`**, устаревшие LDAP paged helper’ы, ряд функций (`money_format()`, `hebrevc()`, …) — полный список: [migration74 deprecated](https://www.php.net/manual/en/migration74.deprecated.php).

<a id="other-changes"></a>
## Прочие изменения и эксплуатация (сборка, INI, perf)

* **`zend.exception_ignore_args`**: новый INI (по умолчанию может скрывать **аргументы** в stack trace — проверьте логирование ошибок).
* **`opcache.preload_user`**: пользователь для preload не из-под root.
* **Миграция на pkg-config**: многие `./configure` больше не принимают `=DIR`; используйте **`PKG_CONFIG_PATH`** или `FOO_CFLAGS`/`FOO_LIBS`.
* **Убраны бандлы**: **SQLite3**, **Zip**, **onig** (mbregex) — нужны системные библиотеки; **hash** всегда включён.
* **CSV**: пустая строка **`$escape`** отключает escape-механизм PHP; поведение `str_getcsv()` согласовано.
* **GD**: поведение `imagecropauto()` приведено к system libgd; изменён дефолтный режим; для **`imagescale()`** `-1` по ширине/высоте сохраняет пропорции.
* **PEAR**: по умолчанию не ставится (`--with-pear` опционально и deprecated).
* **Производительность**: opcode для **`array_key_exists()`** при статическом разрешении; UTF-8 **`preg_match`** валидирует строку один раз при повторных вызовах.

---

<a id="interactive-quiz"></a>
## Интерактивный квиз

Проверьте свои знания о PHP 7.4:

1. **Что произойдет, если попытаться прочитать неинициализированное типизированное свойство класса?**
   * А) Вернется `null`
   * Б) Вызовется Notice, и вернется `null`
   * В) Будет выброшено исключение Error
   * Г) Вернется `false`

   <details>
   <summary>Показать ответ</summary>
   
   **Правильный ответ: В**  
   PHP 7.4 запрещает чтение свойств до их инициализации. Попытка обратиться к такому свойству бросает исключение `Error` с сообщением `Typed property ... must not be accessed before initialization`.
   </details>

2. **Можно ли изменить значение внешней переменной внутри стрелочной функции (arrow function)?**
   * А) Да, переменные передаются по ссылке
   * Б) Нет, переменные захватываются только по значению
   * В) Да, но только при явном использовании слова `use`
   * Г) Да, но только для объектов

   <details>
   <summary>Показать ответ</summary>
   
   **Правильный ответ: Б**  
   Стрелочные функции в PHP захватывают переменные из внешней области видимости по значению. Любые изменения этой переменной внутри функции остаются локальными.
   </details>

3. **Объявление какого типа для свойств класса ЗАПРЕЩЕНО в PHP 7.4?**
   * A) `object`
   * Б) `callable`
   * B) `iterable`
   * Г) `?string`

   <details>
   <summary>Показать ответ</summary>
   
   **Правильный ответ: Б**  
   Тип `callable` не может быть объявлен для свойств класса, поскольку его валидность зависит от контекста вызова.
   </details>

---

<a id="closing-thoughts"></a>
## Итог

Считайте PHP 7.4 **мостом** к 8.x: внедряйте typed properties и современную сериализацию **до** жёстких ошибок восьмёрки. Параллельно прогоните поиск по **целочисленным `PASSWORD_*`**, именам **`fn`**, синтаксису **`$var{idx}`** и шаблонам с **`ArrayObject`** — и прогоните интеграционные тесты для **streams**, **OpenSSL random** и **сериализации PDO/Reflection**.

---
title: "PHP 8.3: типизированные константы, #[Override], json_validate и миграция | DevSense"
description: "PHP 8.3 после 8.2: #[Override], типизированные константы класса, нюансы readonly и clone, json_validate, str_increment/str_decrement, более строгий range() и proc_get_status(), deprecation assert в INI и подводные камни миграции."
faq:
  - question: "Что делает атрибут #[Override] в PHP 8.3?"
    answer: "Атрибут #[Override] указывает, что метод обязательно должен переопределять метод родительского класса или интерфейса. Если метод в родителе переименован или удален, PHP выдаст фатальную ошибку на этапе компиляции."
  - question: "Как работают типизированные константы класса в PHP 8.3?"
    answer: "Константы классов, интерфейсов, трейтов и перечислений теперь могут иметь тип (например, string, int, bool, array). Присваиваемые им значения строго проверяются на соответствие типу при компиляции."
  - question: "В чем преимущество функции json_validate() в PHP 8.3?"
    answer: "json_validate() проверяет валидность строки JSON без её декодирования в структуры PHP. Это значительно экономит процессорное время и оперативную память по сравнению с использованием json_decode()."
  - question: "Как изменилось клонирование readonly-свойств в PHP 8.3?"
    answer: "В свойства с флагом readonly теперь можно повторно записывать значения внутри магического метода `__clone()`, что упрощает глубокое клонирование иммутабельных объектов."
---

# PHP 8.3: Главные нововведения

Выпуск про «полировку» для команд на PHP 8.2.x: сильнее инварианты (`#[Override]`, типизированные константы), удобнее JSON и строки (`json_validate`, `str_increment`/`str_decrement`), плюс длинный хвост мелких изменений runtime, которые проявляются под нагрузкой или в редких ветках кода.

## Содержание (Оглавление)
* [`#[\Override]` — ловим отсутствующие override на этапе компиляции](#override-attribute)
* [Типизированные константы класса](#typed-constants)
* [Readonly: анонимные классы и клонирование](#readonly-tweaks)
* [Улучшения языка (quality-of-life)](#language-qol)
* [Новые функции (`json_validate`, `str_*`, DOM, Random и др.)](#new-functions)
* [Обратно несовместимые изменения (миграционные заметки)](#backward-incompatible)
* [Deprecated (инкремент/декремент строк, `get_class()`, assert INI)](#deprecations)
* [Прочие изменения и эксплуатация (gc_status, streams, highlights)](#other-changes)
* [Частые ошибки](#common-mistakes)
* [Квиз для самопроверки](#self-test-quiz)

---

PHP 8.3 фокусируется на усилении строгости интерфейсных контрактов и оптимизации потребления ресурсов. С введением типизированных констант классов и атрибута `#[\Override]` процесс рефакторинга становится на порядок безопаснее, предотвращая несоответствия в сигнатурах методов на этапе компиляции. Для приложений, обрабатывающих большие объемы внешних данных, новая функция `json_validate()` предлагает легковесный способ валидации JSON без непроизводительных затрат памяти на парсинг. В то же время, рантайм стандартной библиотеки стал строже (особенно это заметно в функции `range()` и сетевых сокетах), а привычные инкременты нечисловых строк теперь официально объявлены deprecated. Ниже приведена инструкция по беспрепятственному переходу.

<a id="override-attribute"></a>
## `#[\Override]` — ловим отсутствующие override на этапе компиляции

Помечайте методы, которые должны переопределять родителя или интерфейс. Если имя ошибочно или метод убрали из родителя, **PHP падает рано**, вместо того чтобы тихо добавить новый метод.

> [!NOTE]
> **А вы знали?**
> Атрибут `#[Override]` полностью игнорируется движком PHP во время выполнения (runtime)! Он предназначен исключительно для проверки на этапе компиляции, помогая вовремя обнаружить ошибки рефакторинга и расхождения в сигнатурах до того, как код будет запущен.

```php
// app/Services/Loggers/FileLogger.php
interface Logger
{
    public function log(string $message): void;
}

final class FileLogger implements Logger
{
    #[\Override]
    public function log(string $message): void
    {
        // ...
    }
}
```

Особенно полезно в больших кодовых базах при рефакторингах интерфейсов.

<a id="typed-constants"></a>
## Типизированные константы класса

Константы в **классах, интерфейсах, трейтах и enum** могут объявлять типы — константы приближаются к остальной системе типов.

```php
// app/Config/AppConfig.php
interface Config
{
    public const string APP_NAME = 'MyApp';
}
```

Меньше ошибок «не тот тип константы», которые раньше всплывали только в местах использования.

<a id="readonly-tweaks"></a>
## Readonly: анонимные классы и клонирование

- **Анонимные классы** могут быть объявлены `readonly`.
- **Readonly-свойства** можно **переинициализировать при `clone`** (внутри метода `__clone()`) — клоны immutable-объектов становятся менее болезненными, чем чисто на паттернах 8.2.

<a id="language-qol"></a>
## Улучшения языка (quality-of-life)

- **Динамический доступ к константе класса**: `SomeClass::{$name}` для имён констант во время выполнения.
- **Инициализаторы static-переменных** могут использовать произвольные выражения (не только константы).
- **`final` у методов трейта** при импорте метода из трейта.
- **Замыкания из magic methods** могут принимать **именованные аргументы** при вызове.
- **`php.ini`**: синтаксис fallback/default для более чистой конфигурации.

<a id="new-functions"></a>
## Новые функции (`json_validate`, `str_*`, DOM, Random и др.)

### `json_validate()`

Проверка JSON **без декодирования** в значения PHP — удобно для API, очередей и быстрой проверки до дорогого `json_decode()`.

> [!NOTE]
> **А вы знали?**
> Функция `json_validate()` потребляет существенно меньше памяти, чем `json_decode()`, так как она не создает никаких массивов или объектов PHP в оперативной памяти! Она лишь сканирует синтаксис строки, что делает её идеальным выбором для валидации больших payloads.

```php
// app/Http/Middleware/JsonValidateMiddleware.php
if (!json_validate($payload)) {
    throw new InvalidArgumentException('Invalid JSON');
}
$data = json_decode($payload, true, flags: JSON_THROW_ON_ERROR);
```

### `str_increment()` / `str_decrement()`

Предпочтительная замена устаревшим `++`/`--` для строк (см. deprecations). Используйте для алфавитно-цифровых счётчиков, где раньше опирались на операторы инкремента.

```php
// app/Services/CounterService.php
$next = str_increment('a9'); // шаг без строкового ++
$prev = str_decrement($next);
```

### DOM, Intl, Random, POSIX и др.

В PHP 8.3 добавлено много методов **DOM** (например `insertAdjacentElement`, `getRootNode`, `replaceChildren`), helper’ов **Intl** для календарей, методов **Random** у `Randomizer` (`getFloat`, `nextFloat`, …) и функций **POSIX** (`sysconf`/`pathconf`/`eaccess`) — полезно для системных скриптов и tooling’а.

<a id="backward-incompatible"></a>
## Обратно несовместимые изменения (миграционные заметки)

### Стек / таймеры / fibers

- **Глубокая рекурсия** у предела стека может выбросить `Error` при превышении `zend.max_allowed_stack_size` (минус резерв); у fibers учитывается `fiber.stack_size`.
- **Zend Max Execution Timers** по умолчанию **включены** для ZTS-сборок на Linux — следите за долгоживущими CLI-воркерами.

### Процессы

- **`proc_get_status()`** на POSIX возвращает корректные значения при **повторных** вызовах; результат может **кэшироваться** (смотрите ключ `"cached"`). **`proc_close()`** после `proc_get_status()` теперь даёт корректный код выхода (не `-1`).

### Массивы и трейты

- **Видимость констант класса**: при наследовании из **интерфейсов** теперь проверяется variance — код с более мягкой видимостью может потребовать правок.
- **Пустой массив + отрицательный первый индекс**: следующий неявный ключ — `n+1` (не `0`).
- **Трейты со static-свойствами**: унаследованные static у родителя **переобъявляются** для каждого использующего трейт класса (отдельное хранилище) — может затронуть тонкое static-состояние.

### Стандартная библиотека (высокий риск)

- **`range()`**: строже валидация (`TypeError`/`ValueError` для плохих входов), больше предупреждений на странных границах, иное поведение для символьных диапазонов, когда границы выглядят как числа — **перепроверьте** код, где диапазоны строятся из пользовательского ввода.
- **`number_format()`**: отрицательный `$decimals` теперь округляет **до** десятичной точки (раньше игнорировалось).
- **`file()`**: неверные комбинации флагов отклоняются (например `FILE_APPEND` раньше молча принимался).

### Date / DOM / FFI / Opcache

- **Date**: более выразительные иерархии **`DateError`/`DateException`** вместо общих предупреждений/исключений — обновите `catch`.
- **DOM**: поведение ближе к спецификации для узлов без родителя; правки **`createAttributeNS()`**; новые члены могут **конфликтовать** с пользовательскими подклассами — проверьте совместимость сигнатур.
- **FFI**: `void` C-функции возвращают **`null`**, а не фиктивный объект `FFI\CData:void`.
- **Opcache**: `opcache.consistency_checks` **удалён** (был несовместим с tracing JIT / кэшем наследования).

### WeakMap

- **Самоссылочные ключи** в `WeakMap` могут собираться в циклах, где достижимость есть только через итерацию — пересмотрите экзотические кэши.

<a id="deprecations"></a>
## Deprecated (инкремент/декремент строк, `get_class()`, assert INI)

### Строковые `++` / `--`

Инкремент/декремент для **пустых или нечисловых** строк помечен deprecated; **нечисловой инкремент** — «мягкий» deprecated. Для нового кода предпочтительны **`str_increment()`** / **`str_decrement()`**.

### `get_class()` / `get_parent_class()` без аргументов

Вызов **без аргументов** deprecated — передавайте объект или имя класса явно.

### Assert

- **`assert_options()`** и связанные **констансты** — deprecated.
- **INI-настройки `assert.*`** — deprecated; миграция описана в разделе migration 8.3 про обработку INI.

### Прочее

- **Reflection**: `ReflectionProperty::setValue($value)` (один аргумент) для static-свойств deprecated — для static передавайте `null` как объект.
- **SQLite3**: предпочтительны исключения; `enableExceptions(false)` даёт deprecation.
- **Random**: вариант `MT_RAND_PHP` deprecated.

<a id="other-changes"></a>
## Прочие изменения и эксплуатация (gc_status, streams, highlights)

- **`gc_status()`** отдаёт больше полей по времени (collector/destructor/free) — полезно при настройке памяти.
- **Streams**: `fread()` на сокетах может вернуть раньше при наличии данных в буфере; memory streams ведут себя ближе к файлам при seek за конец.
- **`open_basedir`**: runtime `ini_set` отклоняет пути с `..` даже с префиксом `./`.
- **highlight_string/file**: изменилась структура HTML — обновите снапшоты тестов, если подсветку сравниваете построчно.

---

## Частые ошибки

Вот несколько распространенных ловушек при переходе на PHP 8.3:

### 1. Использование оператора инкремента (++) на нечисловых строках
Использование операторов `++` или `--` на нечисловых строках в PHP 8.3 вызывает предупреждение об устаревании (deprecation warning).

```php
// app/Services/Counter.php
// ПЛОХО: Вызовет предупреждение устаревания в PHP 8.3
$code = 'a9';
$code++; 

// ХОРОШО: Используйте новую функцию str_increment
$code = str_increment('a9'); // 'b0'
```

### 2. Отсутствие совместимости типов констант в наследниках
Если родительский класс объявляет типизированную константу, дочерние классы при её переопределении должны в точности сохранять её тип.

```php
// app/Config/DatabaseConfig.php
class ParentConfig {
    public const string DRIVER = 'mysql';
}

// ПЛОХО: Fatal Error: ChildConfig::DRIVER type (int) is not compatible with ParentConfig::DRIVER type (string)
class ChildConfig extends ParentConfig {
    public const int DRIVER = 123; 
}
```

### 3. Вызов `get_class()` без аргументов
Вызов `get_class()` без параметров для получения имени текущего класса устарел в PHP 8.3.

```php
// app/Services/LogService.php
// ПЛОХО: Вызовет предупреждение устаревания в PHP 8.3
class LogService {
    public function log(): void {
        echo "Logging from " . get_class(); 
    }
}

// ХОРОШО: Используйте self::class или передавайте $this явно
class LogService {
    public function log(): void {
        echo "Logging from " . self::class; 
        // ИЛИ: echo "Logging from " . get_class($this);
    }
}
```

---

## Квиз для самопроверки

Проверьте свои знания возможностей PHP 8.3. Выберите ответ и сверьтесь под спойлером.

### Вопрос 1: Что произойдет, если в PHP 8.3 объявить константу класса определенного типа и присвоить ей значение другого типа (например, `public const int NAME = 'MyApp';`)?
- A) Будет выдано предупреждение, но код успешно выполнится.
- B) PHP автоматически преобразует строку в целое число `0`.
- C) Произойдет фатальная ошибка (Fatal Error) на этапе компиляции.

<details>
<summary>Показать ответ</summary>

**Правильный ответ: C**
В PHP 8.3 типизированные константы строго проверяются на этапе компиляции. Попытка присвоить константе несовместимое с ее типом значение приводит к Fatal Error.
</details>

### Вопрос 2: В каких случаях предпочтительнее использовать `json_validate()` вместо `json_decode()`?
- A) `json_validate()` работает быстрее и потребляет значительно меньше памяти, поскольку не строит PHP-структур в памяти, а лишь проверяет синтаксис.
- B) `json_validate()` поддерживает расширенные форматы JSON, которые `json_decode()` обработать не может.
- C) `json_decode()` в будущем будет объявлена устаревшей.

<details>
<summary>Показать ответ</summary>

**Правильный ответ: A**
`json_validate()` только сканирует валидность синтаксиса JSON-строки без выделения памяти под распакованные массивы или объекты, что делает её высокопроизводительным решением для чистой проверки валидности данных.
</details>

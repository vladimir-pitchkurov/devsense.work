---
title: "PHP 8.2: новинки — readonly-классы, типы, безопасность и миграция | DevSense"
description: "Гайд по PHP 8.2: readonly-классы, отдельные типы null/false/true, DNF, #[SensitiveParameter], расширение Random, deprecation динамических свойств и обратная несовместимость с 8.1."
faq:
  - question: "Что представляют собой readonly-классы в PHP 8.2?"
    answer: "Объявление класса как readonly делает все его свойства readonly неявно. Все свойства должны быть типизированы, а сам класс не поддерживает динамические свойства. Он может наследоваться только от других readonly-классов."
  - question: "Как #[SensitiveParameter] повышает безопасность приложений?"
    answer: "Атрибут #[SensitiveParameter] скрывает значения чувствительных параметров (пароли, ключи, токены) в стек-трейсах исключений, предотвращая их утечку в файлы логов или вывод ошибок."
  - question: "Что такое DNF (Disjunctive Normal Form) типы в PHP 8.2?"
    answer: "DNF позволяет комбинировать объединения (union) и пересечения (intersection) типов в стандартизированном виде, например `(Countable&Traversable)|null` для сигнатур методов."
  - question: "Почему в PHP 8.2 объявлено устаревшим создание динамических свойств?"
    answer: "Динамические свойства объявлены устаревшими для предотвращения трудноуловимых багов, вызванных опечатками в именах свойств. Классы должны явно объявлять свои свойства, использовать `#[AllowDynamicProperties]` или магические методы."
---

# PHP 8.2: Главные нововведения

Практический обзор для перехода с PHP 8.1.x на 8.2.x: новые возможности языка, атрибуты для защиты секретов в трассировках, ужесточение работы со строками и таймзонами, плюс deprecations, которые лучше закрыть до следующего релиза.

## Содержание (Оглавление)
* [#[SensitiveParameter] и трассировки](#sensitive-parameter)
* [Readonly-классы](#readonly-classes)
* [Отдельные типы (`null`, `false`, `true`) и DNF](#standalone-types-dnf)
* [Константы в трейтах и enum в константных выражениях](#traits-constants)
* [INI: `error_log_mode`](#error-log-mode)
* [Расширение Random](#random-extension)
* [Заметные изменения расширений (cURL, PCRE)](#extension-updates)
* [Обратно несовместимые изменения (миграционные заметки)](#backward-incompatible)
* [Deprecated (исправить до появления ошибок)](#deprecations)
* [Расширения и runtime](#extensions-notes)
* [Частые ошибки](#common-mistakes)
* [Квиз для самопроверки](#self-test-quiz)

---

PHP 8.2 уверенно развивает экосистему в сторону строгой типизации и безопасности структуры данных. С введением readonly-классов и типов в форме DNF (Disjunctive Normal Form), разработчики получили полный контроль над иммутабельностью объектов и сложными декларациями типов. Защита данных теперь встроена прямо на уровне синтаксиса: атрибут `#[SensitiveParameter]` гарантирует, что учетные данные и пароли не окажутся случайно в файлах логов при генерации stack trace. При этом версия 8.2 вносит критически важные изменения — такие как устаревание динамических свойств, требующие внимания при переносе старого кода. Ниже описаны ключевые аспекты миграции.

<a id="sensitive-parameter"></a>
## #[SensitiveParameter] и трассировки

Атрибут `#[\SensitiveParameter]` помечает параметры, которые нужно скрывать в stack trace и отладочном выводе — меньше риска утекания паролей и токенов в логи.

> [!NOTE]
> **А вы знали?**
> Атрибут `#[SensitiveParameter]` нативно поддерживается стандартным механизмом вывода исключений в PHP. Когда PHP генерирует stack trace, он автоматически заменяет значение этого параметра объектом `SensitiveParameterValue`, полностью скрывая исходную строку из логов!

```php
// app/Services/DatabaseService.php
function connect(#[\SensitiveParameter] string $password): void
{
    throw new RuntimeException('fail');
}
```

Сочетайте с политикой логирования и не выводите сырые исключения пользователям в production.

### Практический рецепт: отмечайте секреты на границе

Ставьте `#[\SensitiveParameter]` на параметры, которые могут оказаться в исключениях (обёртки клиентов, auth‑хелперы). Stack trace остаётся полезным, но секреты не «просачиваются» в логи.

```php
// app/Clients/ApiClient.php
final class ApiClient
{
    public function __construct(
        private readonly string $baseUrl,
        #[\SensitiveParameter] private readonly string $token,
    ) {}
}
```

<a id="readonly-classes"></a>
## Readonly-классы

Класс можно объявить `readonly`: все его свойства экземпляра становятся readonly и задаются при конструировании.

> [!NOTE]
> **А вы знали?**
> Readonly-классы автоматически защищают вас от создания динамических свойств! Вам даже не нужно использовать атрибут `#[AllowDynamicProperties]`, поскольку любая попытка добавить динамическое свойство к readonly-классу сразу же вызовет Fatal Error.

```php
// app/DTO/Point.php
readonly class Point
{
    public function __construct(
        public int $x,
        public int $y,
    ) {}
}
```

Удобно для иммутабельных value object и DTO, где объект целиком не должен меняться после создания.

<a id="standalone-types-dnf"></a>
## Отдельные типы (`null`, `false`, `true`) и DNF

В PHP 8.2 **`null`**, **`false`**, **`true`** допустимы как самостоятельные типы в объявлениях.

**DNF** позволяет комбинировать union и intersection в нормализованной форме, например `(A&B)|null`.

```php
// app/Services/ExportService.php
function accepts((Countable&Traversable)|null $c): void {}
```

<a id="traits-constants"></a>
## Константы в трейтах и enum в константных выражениях

- В **трейтах** можно объявлять константы — удобно для общих значений без лишнего базового класса.
- В **константных выражениях** разрешено обращаться к свойствам enum там, где это предусмотрено синтаксисом — богаче конфигурация на этапе компиляции.

<a id="error-log-mode"></a>
## INI: `error_log_mode`

Директива **`error_log_mode`** задаёт права файла error log при создании PHP — полезно для жёстко настроенных серверов.

<a id="random-extension"></a>
## Расширение Random

Расширение **Random** систематизирует генераторы случайных чисел и даёт более предсказуемый API для тестов и криптографически осмысленных сценариев.

### Практический рецепт: `Randomizer` для токенов и выборки

```php
// app/Services/TokenGenerator.php
$engine = new Random\Engine\Mt19937(random_int());
$randomizer = new Random\Randomizer($engine);
$token = $randomizer->getBytesFromString('0123456789abcdef', 32);
```

В тестах задавайте **seeded** engine для воспроизводимости; для секретов выбирайте криптографически подходящий `Random\Engine` по документации.

<a id="extension-updates"></a>
## Заметные изменения расширений (cURL, PCRE)

- **cURL**: `CURLINFO_EFFECTIVE_METHOD`, функция `curl_upkeep()`, новые константы при поддерживаемой libcurl.
- **PCRE**: модификатор **`n` (NO_AUTO_CAPTURE)** — обычные `()` не захватывают, только именованные группы.

<a id="backward-incompatible"></a>
## Обратно несовместимые изменения (миграционные заметки)

### Дата и время

- У `DateTime::createFromImmutable()` / `DateTimeImmutable::createFromMutable()` — tentative return type **`static`**.
- В относительных форматах больше не допускается несколько знаков в `number` (например `+-2`).

### Стандартная библиотека (важно)

- **`strtolower`/`strtoupper`/stri\*/`str_ireplace`/lcfirst/ucfirst/ucwords`**: **не locale-sensitive** — только ASCII, как `"C"`. Локализованный регистр — через **mbstring**.
- **`str_split('')`** теперь возвращает `[]` (раньше был массив с одной пустой строкой).
- **`glob()` / `GlobIterator`**: при `open_basedir` изменилось поведение `[]` vs `false` и предупреждения — проверьте обход каталогов.
- **`FilesystemIterator`**: `SKIP_DOTS` больше не включён по умолчанию через флаги — задайте явно при необходимости.
- **`ksort`/`krsort`** с `SORT_REGULAR` — согласованные правила числовых строк PHP 8.
- **`var_export()`**: имена классов экспортируются с ведущим обратным слэшем (полностью квалифицированные).

### ODBC / PDO_ODBC

Экранирование username/password при дописывании к connection string — проверьте интеграции.

### SPL

У части методов `SplFileObject` ужесточены сигнатуры; у `hasChildren`/`getChildren` — уточнены tentative return types.

**Замечание:** в некоторых окружениях `date_default_timezone_get()` при отсутствии явной зоны может возвращать **UTC** — проверьте предположения о таймзоне.

<a id="deprecations"></a>
## Deprecated (исправить до появления ошибок)

### Динамические свойства

Создание динамических свойств у классов без объявления — **deprecated**, если класс не помечен **`#[\AllowDynamicProperties]`** (или не `stdClass` и т. п.). Лучше объявить свойства, использовать `WeakMap` или магические методы.

### «Относительные» callables

Устарели формы вроде `"self::method"`, часть массивных callables — приведите к стабильным `Class::method` / `[Class::class, 'method']`.

### Интерполяция строк

Стиль `"${var}"` — deprecated; используйте `"{$var}"` или конкатенацию.

### Прочее

- **`utf8_encode` / `utf8_decode`** — deprecated.

<a id="extensions-notes"></a>
## Расширения и runtime

- **OpenSSL**: AEAD для chacha20-poly1305 при наличии поддержки.
- **OCI8**: настройка prefetch LOB для Oracle 12.2+.
- **DBA (LMDB)**: флаги подкаталога при создании БД.

---

## Частые ошибки

Вот несколько распространенных ловушек при использовании возможностей PHP 8.2:

### 1. Создание динамических свойств
В PHP 8.2 попытка присвоить значение необъявленному свойству класса вызывает предупреждение (deprecation warning), а в PHP 9.0 это приведет к фатальной ошибке.

```php
// app/Services/UserService.php
// ПЛОХО: Вызовет предупреждение устаревания в PHP 8.2
class UserService {
    public string $name;
}

$service = new UserService();
$service->email = 'user@example.com'; // Deprecation Warning: Creation of dynamic property is deprecated
```

### 2. Использование устаревшего синтаксиса интерполяции строк
Старый синтаксис `${var}` объявлен устаревшим в PHP 8.2.

```php
// app/Services/NotificationService.php
// ПЛОХО: Вызовет предупреждение устаревания
$name = 'Alice';
echo "Hello ${name}"; // Deprecation Warning

// ХОРОШО: Используйте стандартный синтаксис фигурных скобок
echo "Hello {$name}";
```

### 3. Попытка унаследовать readonly-класс от обычного класса
Класс со спецификатором `readonly` может наследоваться только от другого `readonly`-класса.

```php
// app/DTO/Point3D.php
class Point2D {}

// ПЛОХО: Fatal Error: Readonly class Point3D cannot extend non-readonly class Point2D
readonly class Point3D extends Point2D {}
```

---

## Квиз для самопроверки

Проверьте свои знания возможностей PHP 8.2. Выберите ответ и сверьтесь под спойлером.

### Вопрос 1: Что происходит при объявлении класса как `readonly` в PHP 8.2?
- A) Все его свойства автоматически становятся `readonly`, но указывать их тип не обязательно.
- B) Запрещено добавлять динамические свойства, и все свойства обязательно должны быть типизированными.
- C) Разрешено наследоваться от любого обычного класса, но обычные классы не могут наследоваться от него.

<details>
<summary>Показать ответ</summary>

**Правильный ответ: B**
Объявление класса как `readonly` означает, что все его свойства неявно становятся свойствами только для чтения и должны быть типизированы. Кроме того, в readonly-классах запрещено использование динамических свойств, и наследоваться они могут только от других readonly-классов.
</details>

### Вопрос 2: Какой синтаксис интерполяции вызывает предупреждение устаревания в PHP 8.2?
- A) `"Hello {$name}"`
- B) `"Hello ${name}"`
- C) `"Hello " . $name`

<details>
<summary>Показать ответ</summary>

**Правильный ответ: B**
Синтаксис интерполяции вида `${var}` объявлен устаревшим в PHP 8.2 и вызывает предупреждение. Вместо него следует использовать синтаксис `{$var}`.
</details>

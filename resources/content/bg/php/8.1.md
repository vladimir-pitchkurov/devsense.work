---
title: "PHP 8.1: Enums, Fibers, readonly и intersection types — гайд за ъпгрейд | DevSense"
description: "PHP 8.1 след 8.0: enums, readonly properties, fibers, intersection/never, first-class callables — плюс правила за $GLOBALS, MySQLi изключения и deprecations."
faq:
  - question: "Какво представляват Enums (изброяванията) в PHP 8.1 и как се използват?"
    answer: "Enums предоставят сигурен за типовете (type-safe) начин за дефиниране на затворен набор от стойности. Backed enums свързват случаите с низове или числа за лесно сериализиране, докато pure enums представляват само абстрактни състояния."
  - question: "Какви са правилата за readonly свойствата в PHP 8.1?"
    answer: "Readonly свойствата задължително трябва да са типизирани, не могат да имат стойности по подразбиране и могат да се инициализират само веднъж (обикновено в конструктора). Всеки опит за повторна промяна хвърля Fatal Error."
  - question: "Как работи синтаксисът first-class callable в PHP 8.1?"
    answer: "Той позволява създаване на Closure (затваряне) от всяка функция или метод с помощта на многоточие `...` (например `strlen(...)`), заменяйки по-дългия метод `Closure::fromCallable()`."
  - question: "За какво се използва типът за връщане never?"
    answer: "Типът `never` показва, че функцията никога не връща управлението. Това означава, че тя трябва или да извика `exit()`, или да хвърли изключение, или да влезе в безкраен цикъл, улеснявайки статичния анализ."
---

# PHP 8.1: Основни нововъведения

Път за обновяване от PHP 8.0.x: нови езикови възможности, по-строг runtime и чеклисти за staging.

## Съдържание
* [Enums (Изброявания)](#enums)
* [Readonly properties (Свойства само за четене)](#readonly-properties)
* [First-class callable syntax](#first-class-callable)
* [Fibers (Нишки / Фибри)](#fibers)
* [Intersection types (Сечение на типове)](#intersection-types)
* [never return type](#never-type)
* [new in initializers](#new-in-initializers)
* [Синтактични и езикови подобрения (0o, unpack, named args след unpack)](#syntax-tweaks)
* [Обратно несъвместими промени (бележки за миграция)](#backward-incompatible)
* [Deprecated (какво е добре да оправите рано)](#deprecations)
* [Extensions: важни промени](#extensions-changes)
* [Чести грешки](#common-mistakes)
* [Квиз за самопроверка](#self-test-quiz)

---

Ако PHP 8.0 нулира платформата, версия 8.1 я изпълва с ежедневни практически абстракции. С въвеждането на нативните изброявания (Enums), readonly свойствата и сеченията на типове директно в езика, разработчиците вече могат да пишат изключително чист и сигурен код без излишен boilerplate. Въпреки това, runtime средата става по-строга: забранено е директното презаписване на `$GLOBALS`, MySQLi по подразбиране хвърля изключения, а голям брой legacy практики вече са маркирани като остарели (deprecated). Ето подробен и практичен наръчник за новите възможности и успешния преход.

<a id="enums"></a>
## Enums

Enums са първокласен, type-safe начин да представите “затворен” набор от стойности.

- **Backed enums** пазят скаларна стойност (`string|int`), удобна за съхранение/пренасяне през API.
- **Pure enums** са само идентичност — отлични за домейн състояния и изчерпателна обработка.

> [!NOTE]
> **А знаехте ли?**
> Под капака изброяванията (Enums) в PHP са имплементирани като класове! Това означава, че те могат да реализират интерфейси, да съдържат методи (включително статични) и да използват трейтове (с ограничението, че трейтовете не могат да съдържат свойства) точно като обикновените PHP класове.

```php
// app/Enums/Status.php
enum Status: string
{
    case Draft = 'draft';
    case Published = 'published';
    case Archived = 'archived';
}

// app/Services/PostService.php
function canEdit(Status $status): bool
{
    return $status !== Status::Archived;
}

$status = Status::from('draft');  // Status::Draft
$value  = $status->value;         // 'draft'
```

**Практични насоки**

- Използвайте enums вместо “string флагове” (`'draft'|'published'`) в сигнатури и DTO.
- За БД и публични API често са по-удобни backed enums; за вътрешни workflow състояния — pure enums.
- Комбинирайте с `match` за изчерпателна обработка:

```php
// app/Services/PostService.php
function label(Status $status): string
{
    return match ($status) {
        Status::Draft => 'Draft',
        Status::Published => 'Published',
        Status::Archived => 'Archived',
    };
}
```

<a id="readonly-properties"></a>
## Readonly properties

Readonly properties могат да бъдат присвоени **само веднъж** — най-често в конструктора. Това е голям плюс за immutable DTO, value objects и по-сигурни домейн модели.

> [!NOTE]
> **А знаехте ли?**
> Свойствата с маркер readonly не могат да имат стойност по подразбиране! Ако се опитате да напишете `public readonly string $status = 'draft';`, PHP ще хвърли фатална грешка още при компилация.

```php
// app/DTO/UserProfile.php
final class UserProfile
{
    public function __construct(
        public readonly int $id,
        public readonly string $email,
    ) {}
}
```

**Какво променя архитектурно**

- Насърчава обекти “конструираш напълно — после не мутираш”.
- Намалява нуждата от private + getter-и за DTO-подобни типове.
- Работи чудесно с constructor property promotion.

<a id="first-class-callable"></a>
## First-class callable syntax

PHP 8.1 добавя удобен синтаксис за създаване на closures от callables:

```php
// app/Helpers/StringHelper.php
function normalize(string $s): string
{
    return strtolower(trim($s));
}

// app/Services/DataService.php
$fn = normalize(...);                 // Closure
$out = array_map($fn, [' A ', 'B ']); // ['a', 'b']
```

Еквивалентно е на `Closure::fromCallable('normalize')`, но е по-кратко и се чете по-добре в pipeline-и.

<a id="fibers"></a>
## Fibers

Fibers са ниско-нивов примитив за **кооперативна конкурентност**. Те сами по себе си не правят кода async, но позволяват на библиотеки/фреймворкове да изграждат async runtime-и, scheduler-и и structured concurrency.

```php
// app/Services/FiberService.php
$fiber = new Fiber(function (): void {
    $value = Fiber::suspend('paused');
    echo "resumed with: {$value}\n";
});

echo $fiber->start() . "\n"; // paused
$fiber->resume('payload');   // resumed with: payload
```

**Кога има значение**

- Ако използвате async framework/event loop (ReactPHP, Amp): Fibers могат да улеснят интеграциите.
- За класически request/response приложения Fibers са предимно “инфраструктура за библиотеки”.

<a id="intersection-types"></a>
## Intersection types

Intersection types означават “трябва да удовлетворява всички тези интерфейси/класове едновременно”.

```php
// app/Services/ExporterService.php
function export(iterable&Countable $items): array
{
    if (count($items) === 0) {
        return [];
    }

    return iterator_to_array($items, preserve_keys: true);
}
```

Важно: intersection types не могат да се комбинират с union types в PHP 8.1 (DNF типовете се въвеждат по-късно в 8.2).

<a id="never-type"></a>
## never return type

Return type `never` означава, че функцията **никога не връща** управление: тя винаги хвърля exception или извиква exit.

```php
// app/Helpers/ErrorHelper.php
function fail(string $message): never
{
    throw new RuntimeException($message);
}
```

Това подобрява статичния анализ и прави control flow по-ясен.

<a id="new-in-initializers"></a>
## new in initializers

PHP 8.1 позволява `new` изрази на повече места: defaults на параметри, static variables, инициализатори на константи и аргументи на атрибути.

```php
// app/Services/Clock.php
final class Clock
{
    public function now(): DateTimeImmutable
    {
        return new DateTimeImmutable('now');
    }
}

// app/Services/HandlerService.php
function handler(Clock $clock = new Clock()): DateTimeImmutable
{
    return $clock->now();
}
```

Това премахва доста “factory boilerplate” в простите случаи. (В DI контейнер внимавайте с lifetime-а на услугите.)

<a id="syntax-tweaks"></a>
## Синтактични и езикови подобрения (0o, unpack, named args след unpack)

### Octal integer literal prefix: `0o` / `0O`

```php
// app/Config/FileConfig.php
$mask = 0o755;
```

### Array unpacking with string keys

```php
// app/Services/ArrayService.php
$a = [1, 'a' => 'b'];
$b = [...$a, 'c' => 'd']; // [1, 'a' => 'b', 'c' => 'd']
```

### Named argument after argument unpacking

```php
// app/Services/ArgumentService.php
foo(...$args, named: $arg);
```

### full_path for file uploads

В `$_FILES` вече има ключ `full_path` (основно за `webkitdirectory` uploads).

### Практични рецепти

#### Backed enum от ненадежден вход (`tryFrom`)

```php
// app/Http/Controllers/StatusController.php
$status = Status::tryFrom($row['status']) ?? Status::Draft;
```

#### Пайплайни с first-class callable

```php
// app/Services/PipelineService.php
$normalize = static fn(string $s): string => strtolower(trim($s));
$mapper = $normalize(...);
$lines = array_map($mapper, $raw);
```

<a id="backward-incompatible"></a>
## Обратно несъвместими промени (бележки за миграция)

Този раздел е прагматичен: фокусира се върху неща, които най-често реално чупят ъпгрейда.

### PHP core / language

- **Ограничения за запис в $GLOBALS**: запис към “целия” `$GLOBALS` вече не е позволен (напр. `array_pop($GLOBALS)` дава error). Достъпът до елементи `$GLOBALS['x']` продължава да работи.
- **static variables в наследени методи**: наследените (не override-нати) методи вече споделят static variables с родителския метод.
- **Optional преди required параметри**: optional параметри, декларирани преди required, се третират като required; в PHP 8.1 това може да доведе до `ArgumentCountError` при извикване (дори с named arguments).
- **Return type compatibility с internal classes**: override на вътрешни методи без съвместим return type води до deprecation; за cross-version използвайте `#[ReturnTypeWillChange]`.
- **Нови ключови думи**:
  - `readonly` вече е keyword (но все още може да се използва като име на функция).
  - `never` вече е reserved word (не може да се използва за имена на class/interface/trait и е забранено в namespaces).

### Resource → object migration (още)

Няколко разширения мигрират ресурси към обекти (проверки с `is_resource()` заменете с проверки за `false` или с object types):

- **FileInfo**: `finfo` objects вместо `fileinfo` resources.
- **FTP**: `FTP\Connection` objects.
- **IMAP**: `IMAP\Connection` objects.
- **LDAP**: `LDAP\Connection`, `LDAP\Result`, `LDAP\ResultEntry` objects.
- **PgSQL**: `PgSql\Connection`, `PgSql\Result`, `PgSql\Lob` objects.
- **PSpell**: `PSpell\Dictionary` и `PSpell\Config` objects.

### PDO / MySQLi behavior changes

- **MySQLi**: default error reporting mode се промени от “silent” на “exceptions”. За да върнете старото поведение: `mysqli_report(MYSQLI_REPORT_OFF);`
- **PDO**: промени в `PDO::ATTR_STRINGIFY_FETCHES` за bool; SQLite/MySQL драйверите по-консистентно връщат ints/floats като нативни PHP типове.

### Standard library behavior changes

- `version_compare()` вече не приема недокументирани съкращения за оператори.
- HTML escaping функциите по подразбиране използват `ENT_QUOTES | ENT_SUBSTITUTE` (кавичките се escape-ват; невалиден UTF-8 се заменя).

<a id="deprecations"></a>
## Deprecated (какво е добре да оправите рано)

Deprecations в 8.1 са “бъдещи грешки”. Ако ги оправите сега, следващите ъпгрейди към PHP 8.2+ ще са много по-лесни.

### Core deprecations

- Подаване на `null` към non-nullable параметри на built-in функции е deprecated.
- Неявни lossy float → int конверсии са deprecated (array keys, int type declarations в coercive mode, int оператори).
- Извикване на static елемент върху trait е deprecated.
- Autovivification от `false` е deprecated (`$x = false; $x[] = 1;`).

### Notable extension deprecations

- **Date**: `strftime()` / `gmstrftime()` и `strptime()` са deprecated (предпочитайте `date()` / `date_parse_from_format()` / `IntlDateFormatter`).
- **Filter**: `FILTER_SANITIZE_STRING` и `FILTER_SANITIZE_STRIPPED` са deprecated.
- **Hash**: `mhash*()` са deprecated (използвайте `hash_*()`).
- **PDO**: `PDO::FETCH_SERIALIZE` е deprecated.

<a id="extensions-changes"></a>
## Extensions: важни промени

Тук са събрани “добре е да се знае” промени, които не винаги чупят кода директно, но влияят на поведение и операции.

- **Reflection**: `ReflectionProperty::setAccessible()` / `ReflectionMethod::setAccessible()` вече нямат ефект (всичко е “достъпно” през Reflection).
- **Phar**: SHA256 е default за подписи; поддържат се OpenSSL_SHA256/OpenSSL_SHA512 подписи.
- **OpenSSL**: OpenSSL 3.0 е поддържан; наличността на cipher-и и валидирането на параметри са по-строги.
- **CLI**: `php -a` без readline вече дава error.
- **PHPDBG**: remote функционалността е премахната.

<a id="common-mistakes"></a>
## Чести грешки

Ето няколко често срещани капана при използването на новите възможности на PHP 8.1:

### 1. Промяна на Readonly свойства вътре в класа или в наследници
Свойство с маркер `readonly` не може да бъде презаписано дори от самия клас или негови наследници, след като веднъж е инициализирано.

```php
// app/DTO/UserProfile.php
// ЛОШО: Модификация на readonly свойство след инициализация ще доведе до Error
class UserProfile {
    public function __construct(public readonly string $email) {}

    public function updateEmail(string $newEmail): void {
        $this->email = $newEmail; // Fatal Error: Cannot modify readonly property
    }
}
```

### 2. Автоматично създаване на масив от false (Autovivification from False)
В PHP 8.1 опитът за автоматично изграждане на масив от булева променлива със стойност `false` е обявен за остарял (deprecated).

```php
// app/Services/ArrayService.php
// ЛОШО: Ще предизвика предупреждение за остарялост в PHP 8.1 (и фатална грешка в PHP 8.2)
$data = false;
$data[] = 'value';

// ДОБРЕ: Първо инициализирайте променливата като празен масив
$data = [];
$data[] = 'value';
```

### 3. Презаписване на масива $GLOBALS
Можете да променяте променливите в `$GLOBALS`, но не можете да презаписвате или напълно да нулирате самата глобална променлива `$GLOBALS`.

```php
// app/Services/LegacyService.php
// ЛОШО: Ще хвърли Fatal Error в PHP 8.1
$GLOBALS = []; 

// ЛОШО: Презаписване чрез референция също е забранено
$GLOBALS = &$someOtherArray;
```

<a id="self-test-quiz"></a>
## Квиз за самопроверка

Проверете знанията си за възможностите на PHP 8.1. Изберете отговор и проверете под спойлера.

### Въпрос 1: Кое от следните е вярно за Readonly свойствата в PHP 8.1?
- A) Те могат да бъдат презаписани, стига новата стойност да е от същия тип.
- B) Те могат да се декларират само върху типизирани свойства (без тип не е позволено).
- C) Може да им се зададе стойност по подразбиране при дефиниране на класа.

<details>
<summary>Покажи отговора</summary>

**Правилен отговор: B**
В PHP 8.1 маркерът `readonly` може да се прилага само към типизирани свойства. Декларирането на readonly свойство без тип е синтактична грешка. Освен това те нямат право на стойности по подразбиране и никога не могат да бъдат презаписани след първата инициализация.
</details>

### Въпрос 2: Какво се случва при опит за автоматично създаване на масив от променлива, която в момента е `false` (например `$x = false; $x[] = 1;`) в PHP 8.1?
- A) Всичко работи без проблем, като променливата се преобразува в масив.
- B) Задейства се предупреждение за остарял синтаксис (deprecation warning).
- C) Веднага се хвърля фатална грешка (Fatal Error).

<details>
<summary>Покажи отговора</summary>

**Правилен отговор: B**
В PHP 8.1 автоматичното изграждане на масив от `false` е остаряло поведение и генерира предупреждение. В PHP 8.2 това вече се наказва с Fatal Error.
</details>

---
title: "PHP 8.3: типизирани константи, #[Override], json_validate и миграция | DevSense"
description: "PHP 8.3 след 8.2: #[Override], типизирани константи на класа, нюанси readonly и clone, json_validate, str_increment/str_decrement, по-строги range() и proc_get_status(), deprecation на assert в INI и капани при миграция."
faq:
  - question: "Какво прави атрибутът #[Override] в PHP 8.3?"
    answer: "Атрибутът #[Override] указва, че методът задължително трябва да препокрива метод в родителския клас или интерфейс. Ако родителският метод бъде преименуван или изтрит, PHP ще генерира Fatal Error още при компилация."
  - question: "Как работят типизираните константи на клас в PHP 8.3?"
    answer: "Константите на класове, интерфейси, трейтове и енумерации вече могат да имат деклариран тип (например string, int, bool, array). Стойностите им се проверяват строго при компилация."
  - question: "Каква е ползата от json_validate() в PHP 8.3?"
    answer: "json_validate() проверява дали даден низ е валиден JSON без да го декодира в PHP структури. Това спестява значително количество процесорно време и памет в сравнение с json_decode()."
  - question: "Как се променя клонирането на readonly свойства в PHP 8.3?"
    answer: "Readonly свойствата вече могат да бъдат реинициализирани (променяни) в рамките на магическия метод `__clone()`, което улеснява дълбокото клониране на имютабилни обекти."
---

# PHP 8.3: Основни нововъведения

Издание за „полиране“ за екипи на PHP 8.2.x: по-силни инварианти (`#[Override]`, типизирани константи), по-удобен JSON и низове (`json_validate`, `str_increment`/`str_decrement`), плюс дълга опашка от дребни промени в runtime, които излизат наяве под натоварване или в редки клонове на кода.

## Съдържание
* [`#[\Override]` — липсващи override още при компилация](#override-attribute)
* [Типизирани константи на класа](#typed-constants)
* [Readonly: анонимни класове и клониране](#readonly-tweaks)
* [Подобрения в езика (quality-of-life)](#language-qol)
* [Нови функции (`json_validate`, `str_*`, DOM, Random и др.)](#new-functions)
* [Обратно несъвместими промени (миграционни бележки)](#backward-incompatible)
* [Deprecated (инкремент/декремент на низове, `get_class()`, assert INI)](#deprecations)
* [Други промени и експлоатация (gc_status, streams, highlights)](#other-changes)
* [Чести грешки](#common-mistakes)
* [Квиз за самопроверка](#self-test-quiz)

---

PHP 8.3 е изцяло насочен към засилване сигурността на интерфейсните договори и оптимизирането на ресурсите. Чрез въвеждането на типизирани константи на класове и атрибута `#[\Override]`, PHP прави рефакторирането на кода много по-безопасно и предотвратява несъответствия в сигнатурите на методите още по време на компилация. За приложения, обработващи големи обеми от външни данни, новата функция `json_validate()` предлага лек начин за валидиране на JSON без излишно разхищение на памет за парсване. В същото време, runtime средата на стандартната библиотека става по-строга (особено при функцията `range()` и мрежовите сокети), а традиционният инкремент на нечислови низове вече е официално deprecated. Ето как да преминете безпроблемно към новата версия.

<a id="override-attribute"></a>
## `#[\Override]` — липсващи override още при компилация

Маркирайте методи, които трябва да override-ват родител или интерфейс. Ако името е грешно или методът е премахнат от родителя, **PHP гърми рано**, вместо тихо да добави нов метод.

> [!NOTE]
> **А знаехте ли?**
> Атрибутът `#[Override]` се игнорира напълно от PHP енджина по време на изпълнение (runtime)! Той е предназначен изцяло за проверка по време на компилация, помагайки за откриване на грешки при рефакторинг и несъответствия в интерфейсите преди стартиране на кода.

```php
// app/Services/Loggers/FileLogger.php
interface Logger
{
    public function log(string $message): void;
}

final class FileLogger implements Logger
{
    #[\Override]
    public function log(string $message): void
    {
        // ...
    }
}
```

Особено полезно в големи кодови бази при рефакторинг на интерфейси.

<a id="typed-constants"></a>
## Типизирани константи на класа

Константи в **класове, интерфейси, трейтове и enum** могат да декларират типове — константите се подравняват с останалата типова система.

```php
// app/Config/AppConfig.php
interface Config
{
    public const string APP_NAME = 'MyApp';
}
```

По-малко грешки „грешен тип на константа“, които преди излизаха на повърхност едва при употреба.

<a id="readonly-tweaks"></a>
## Readonly: анонимни класове и клониране

- **Анонимните класове** могат да бъдат декларирани `readonly`.
- **Readonly свойства** могат да се **реинициализират при `clone`** (вътре в магическия метод `__clone()`), когато графът от обекти го позволява — immutable клонове стават по-малко болезнени от чисто 8.2 шаблони.

<a id="language-qol"></a>
## Подобрения в езика (quality-of-life)

- **Динамичен достъп до константа на клас**: `SomeClass::{$name}` за имена на константи по време на изпълнение.
- **Инициализатори на static променливи** могат да използват произволни изрази (не само константи).
- **`final` върху методи от трейт** при импорт на метод от трейт.
- **Затваряния от magic methods** могат да приемат **именувани аргументи** при извикване.
- **`php.ini`**: синтаксис fallback/default за по-чиста конфигурация.

<a id="new-functions"></a>
## Нови функции (`json_validate`, `str_*`, DOM, Random и др.)

### `json_validate()`

Валидиране на JSON **без декодиране** към PHP стойности — удобно за API, опашки и бърза проверка преди скъп `json_decode()`.

> [!NOTE]
> **А знаехте ли?**
> Използването на `json_validate()` консумира значително по-малко памет от `json_decode()`, тъй като не изгражда масиви или обекти в паметта! Тя просто сканира низовия синтаксис, което я прави идеална за валидиране на големи payloads преди обработка.

```php
// app/Http/Middleware/JsonValidateMiddleware.php
if (!json_validate($payload)) {
    throw new InvalidArgumentException('Invalid JSON');
}
$data = json_decode($payload, true, flags: JSON_THROW_ON_ERROR);
```

### `str_increment()` / `str_decrement()`

Предпочитана замяна на остарелите `++`/`--` върху низове (виж deprecations). Използвайте за буквено-цифрови броячи, където преди разчитахте на оператори за инкремент.

```php
// app/Services/CounterService.php
$next = str_increment('a9'); // стъпка без string ++
$prev = str_decrement($next);
```

### DOM, Intl, Random, POSIX и др.

В PHP 8.3 са добавени много **DOM** методи (напр. `insertAdjacentElement`, `getRootNode`, `replaceChildren`), **Intl** помощници за календари, **Random** методи към `Randomizer` (`getFloat`, `nextFloat`, …) и **POSIX** функции (`sysconf`/`pathconf`/`eaccess`) — помощни за системни скриптове и tooling.

<a id="backward-incompatible"></a>
## Обратно несъвместими промени (миграционни бележки)

### Стек / таймери / fibers

- **Дълбока рекурсия** близо до лимита на стека може да хвърли `Error` при надхвърляне на `zend.max_allowed_stack_size` (минус резерв); при fibers важи `fiber.stack_size`.
- **Zend Max Execution Timers** по подразбиране **включени** за ZTS build-ове на Linux — внимавайте с дълго живеещи CLI worker-и.

### Процеси

- **`proc_get_status()`** на POSIX връща коректни стойности при **повторни** извиквания; резултатът може да се **кешира** (ключ `"cached"`). **`proc_close()`** след `proc_get_status()` вече дава коректен exit code (не `-1`).

### Масиви и трейтове

- **Видимост на константи на класа**: при наследяване от **интерфейси** сега се налага variance — код с по-мека видимост може да изисква корекции.
- **Празен масив + отрицателен първи индекс**: следващият неявен ключ е `n+1` (не `0`).
- **Трейтове със static свойства**: наследените static от родителя се **пренареждат** за всеки клас, ползващ трейта (отделно хранилище) — може да засегне фино static състояние.

### Стандартна библиотека (висок риск)

- **`range()`**: по-строга валидация (`TypeError`/`ValueError` за лоши входове), повече предупреждения при странни граници, различно поведение за символни диапазони, когато границите изглеждат числови — **претествайте** код, където диапазоните идват от потребителски вход.
- **`number_format()`**: отрицателен `$decimals` сега закръглява **преди** десетичната точка (преди се игнорираше).
- **`file()`**: невалидни комбинации от флагове се отхвърлят (напр. `FILE_APPEND` преди се приемаше мълчаливо).

### Date / DOM / FFI / Opcache

- **Date**: по-изразителни йерархии **`DateError`/`DateException`** вместо общи предупреждения/изключения — обновете `catch`.
- **DOM**: поведение по-близо до спецификацията за възли без родител; корекции в **`createAttributeNS()`**; нови членове могат да **конфликтират** с потребителски подкласове — проверете съвместимост на сигнатури.
- **FFI**: `void` C функции връщат **`null`**, а не фиктивен обект `FFI\CData:void`.
- **Opcache**: `opcache.consistency_checks` е **премахнат** (беше несъвместим с tracing JIT / кеша на наследяване).

### WeakMap

- **Самореферентни ключове** в `WeakMap` могат да се събират в цикли, където достижимостта е само чрез итерация — прегледайте екзотични кешове.

<a id="deprecations"></a>
## Deprecated (инкремент/декремент на низове, `get_class()`, assert INI)

### Низови `++` / `--`

Инкремент/декремент върху **празни или нечислови** низове е deprecated; **нечислов инкремент** е „мек“ deprecated. За нов код предпочитайте **`str_increment()`** / **`str_decrement()`**.

### `get_class()` / `get_parent_class()` без аргументи

Извикване **без аргументи** е deprecated — подавайте обект или име на клас изрично.

### Assert

- **`assert_options()`** и свързаните **константи** са deprecated.
- **INI настройките `assert.*`** са deprecated; миграцията е описана в migration 8.3 за обработка на INI.

### Друго

- **Reflection**: `ReflectionProperty::setValue($value)` (един аргумент) за static свойства е deprecated — за static подайте `null` като обект.
- **SQLite3**: предпочитайте изключения; `enableExceptions(false)` дава deprecation.
- **Random**: вариантът `MT_RAND_PHP` е deprecated.

<a id="other-changes"></a>
## Други промени и експлоатация (gc_status, streams, highlights)

- **`gc_status()`** връща повече полета за време (collector/destructor/free) — полезно при настройка на памет.
- **Streams**: `fread()` върху сокети може да върне по-рано при буферирани данни; memory streams се държат по-близо до файлове при seek след края.
- **`open_basedir`**: runtime `ini_set` отхвърля пътища с `..` дори с префикс `./`.
- **highlight_string/file**: променена е HTML структурата — обновете snapshot тестове, ако сравнявате подсветката.

---

## Чести грешки

Ето няколко често срещани капана при използването на новите възможности на PHP 8.3:

### 1. Използване на оператора инкремент (++) върху низове
В PHP 8.3 използването на операторите `++` или `--` върху нечислови низове задейства предупреждение за остарял синтаксис (deprecation warning).

```php
// app/Services/Counter.php
// ЛОШО: Задейства предупреждение в PHP 8.3
$code = 'a9';
$code++; 

// ДОБРЕ: Използвайте новата помощна функция str_increment
$code = str_increment('a9'); // 'b0'
```

### 2. Липса на съвместимост с типа на константата в подкласове
Ако родителски клас дефинира типизирана константа, дочерните класове, които я препокриват, трябва да запазят типа абсолютно същия.

```php
// app/Config/DatabaseConfig.php
class ParentConfig {
    public const string DRIVER = 'mysql';
}

// ЛОШО: Fatal Error: ChildConfig::DRIVER type (int) is not compatible with ParentConfig::DRIVER type (string)
class ChildConfig extends ParentConfig {
    public const int DRIVER = 123; 
}
```

### 3. Извикване на `get_class()` без аргументи
Извикването на `get_class()` без предоставени аргументи с цел получаване на името на текущия клас е deprecated в PHP 8.3.

```php
// app/Services/LogService.php
// ЛОШО: Задейства предупреждение за остарял синтаксис в PHP 8.3
class LogService {
    public function log(): void {
        echo "Logging from " . get_class(); 
    }
}

// ДОБРЕ: Използвайте self::class или подайте $this изрично
class LogService {
    public function log(): void {
        echo "Logging from " . self::class; 
        // ИЛИ: echo "Logging from " . get_class($this);
    }
}
```

---

## Квиз за самопроверка

Проверете знанията си за възможностите на PHP 8.3. Изберете отговор и проверете под спойлера.

### Въпрос 1: Какво се случва в PHP 8.3, ако дефинирате константа на клас с тип и опитате да ѝ присвоите стойност от друг тип (напр. `public const int NAME = 'MyApp';`)?
- A) Задейства се предупреждение (deprecation warning), но кодът се изпълнява успешно.
- B) PHP автоматично конвертира низа до цяло число `0`.
- C) Хвърля се фатална грешка (Fatal Error) още при компилация.

<details>
<summary>Покажи отговора</summary>

**Правилен отговор: C**
В PHP 8.3 типизираните константи на класове се проверяват строго по време на компилация. Присвояването на несъвместима стойност води до Fatal Error.
</details>

### Въпрос 2: Защо трябва да използвате `json_validate()`, вместо `json_decode()`, ако искате само да проверите дали даден низ е валиден JSON?
- A) `json_validate()` работи по-бързо и изразходва значително по-малко памет, тъй като не конструира PHP масиви и обекти в паметта.
- B) `json_validate()` поддържа JSON формати, които `json_decode()` не умее да парсне.
- C) `json_decode()` ще бъде обявен за остарял в бъдещите версии на PHP.

<details>
<summary>Покажи отговора</summary>

**Правилен отговор: A**
`json_validate()` просто проверява синтактичната коректност на JSON низа без да заделя памет за асоциирани структури, което я прави много по-бърза за чиста валидация.
</details>
